# AlternativeTo — Submission (EN)

> Dán từng mục vào form **Suggest new application** trên https://alternativeto.net
> (menu user góc phải trên → Suggest new application). Cần verify email trước.

## Trước khi submit

- Search "Elasticvix" trên site — nếu đã có listing thì claim, đừng tạo trùng.
- Không dùng profile/comment để quảng cáo sản phẩm — account làm vậy bị block spam.
- Queue duyệt mặc định vài tháng; $5 một lần để lên đầu hàng (thường 1–2 ngày làm việc,
  không đảm bảo được duyệt).
- Lý do bị loại phổ biến: app low-effort, không khác gì cái đã có, category bão hoà.
  → Nhấn điểm khác biệt (autocomplete/linting đọc mapping thật của cluster) ngay đoạn mở.
- Theo dõi trạng thái: profile → **My submissions**.

## Name

Elasticvix

## Tagline / short description

Elasticsearch client and GUI in your browser — query console with field-aware autocomplete, search UI, saved queries, multi-cluster.

## Platforms

Google Chrome (browser extension). Thêm Microsoft Edge / Chromium nếu form có — cùng bản
CWS cài được. Không chọn Firefox (chưa hỗ trợ).

## License / Pricing

Open Source — MIT. Free.

## Category

Developer Tools

## Links

- Official website: https://totanvix.github.io/elasticvix/
- Download: https://chromewebstore.google.com/detail/elasticvix-elasticsearch/glnbabapnpecmdaekagajnedgkbhcgad
- Source code: https://github.com/totanvix/elasticvix

## Description

Elasticvix is an Elasticsearch client and GUI that runs entirely in your browser. Connect and start querying in seconds — no server, no desktop app, no account.

What sets it apart: the autocomplete and linting read your actual cluster. Instead of static keyword lists, you get real field names and real values from your own indices as you type, and any field missing from the mapping is flagged before you run the query.

• Query console — Query DSL autocomplete from your index mappings, field linting, foldable/filterable response viewer, Cmd+Enter to run
• Search UI — pick indices, browse hits in a sortable table, view/edit/delete documents, run aggregations, export JSON
• Cluster overview — health, version, shard/index/doc counts, per-node RAM, JVM heap, disk and CPU
• Saved queries & history — name and tag queries, every request kept automatically
• Multi-cluster — unlimited connections, instant switching, auth via basic, API key or bearer token
• Works with Elasticsearch 6.x, 7.x, 8.x (tested) and 9.x (best effort)

All data stays on your machine. Connections, credentials, saved queries and history live in your browser and are sent only to the clusters you configure. No analytics, no tracking. Free and open source under the MIT License.

## Tags

elasticsearch, elasticsearch-client, elasticsearch-gui, chrome-extension, query-console, search, database-client, devtools, json, self-hosted

## Alternative to

Elasticvue, Kibana, Cerebro, Dejavu, ElasticHQ, Elasticsearch Head

> Mục quan trọng nhất cho traffic — người dùng vào AlternativeTo là để tìm thay thế cho
> một app cụ thể.

## Features (điền sau khi listing được duyệt)

AlternativeTo cho lọc app theo feature — thiếu mục này giảm hiển thị.

- Autocomplete from index mappings
- Query DSL linting
- Multi-cluster support
- Saved queries
- Query history
- Dark mode
- Works offline (local clusters)
- No account required
- Privacy focused

## Assets

- Icon: `icon.svg` (xuất PNG nếu form không nhận SVG)
- Screenshots: `screenshots/01-search.png`, `screenshots/02-console-autocomplete.png`,
  `screenshots/03-cluster.png`, `screenshots/05-dark-mode.png`

## Sau khi được duyệt

- Claim listing bằng nút "I'm the developer" để tự sửa description, thêm feature, trả lời comment.
- Không tự vote hoặc tạo account ảo — bị phát hiện là xoá listing.
